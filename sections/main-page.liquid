<section class="section">
  <div class="container">
    <div class="section__head reveal">
      <span class="section__eyebrow">{{ shop.name }}</span>
      <h1 class="section__title">{{ page.title }}</h1>
    </div>
    <div class="page-content rte reveal">
      {{ page.content }}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Page",
  "settings": []
}
{% endschema %}
